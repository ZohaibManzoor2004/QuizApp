// import { Pool } from "pg";
// import { drizzle } from "drizzle-orm/pg"; // this is correct for PostgreSQL

// const pool = new Pool({
//   connectionString: process.env.DATABASE_URL,
// });

// export const db = drizzle(pool);
